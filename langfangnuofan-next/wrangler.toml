name = "langfangnuofan-next"
pages_build_output_dir = ".vercel/output/static"
compatibility_date = "2026-06-01"
compatibility_flags = ["nodejs_compat"]

# D1 binding configured via Cloudflare Pages Dashboard (Settings > Functions > D1 Database Bindings)
